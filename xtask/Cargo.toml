[package]
name = "xtask"
description = "Maintainer tasks for the oxidor workspace (not published)"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
prost-build = "0.14.4"
protox = "0.9.1"
